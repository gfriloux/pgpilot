---
title: Installation
description: Install pgpilot on Linux — from GitHub releases or building from source.
---

pgpilot is a graphical key manager for GPG. This guide covers installation from GitHub releases and building from source.

## Prerequisites

pgpilot requires:

- **GnuPG** ≥ 2.2 (the `gpg` command must be in `PATH`)
- **gpg-agent** (usually bundled with GnuPG)
- **pinentry** — a pinentry program for password prompts (e.g. `pinentry-tty`, `pinentry-gtk2`, or `pinentry-qt`)

### Verify prerequisites (Linux)

```bash
gpg --version
# Output should show GnuPG version ≥ 2.2

gpg-agent --version
# Confirm agent is available

which pinentry
# Should find a pinentry binary (or check: gpg-conf --list-dirs | grep pinentry)
```

If `gpg` is missing:

- **Ubuntu / Debian**: `sudo apt install gpg gnupg-agent pinentry`
- **Fedora / RHEL**: `sudo dnf install gnupg pinentry`
- **Arch**: `sudo pacman -S gnupg pinentry`
- **NixOS**: Use `nix-shell -p gnupg gpg-agent pinentry` or add to `flake.nix`/`shell.nix`

---

## Installation from GitHub Releases

1. Visit [pgpilot releases](https://github.com/gfriloux/pgpilot/releases)
2. Download the latest `pgpilot` binary for your Linux distribution (amd64, arm64, etc.)
3. Make it executable:
   ```bash
   chmod +x pgpilot
   ```
4. Run it:
   ```bash
   ./pgpilot
   ```

To install system-wide:

```bash
sudo mv pgpilot /usr/local/bin/
pgpilot  # now runs from anywhere
```

---

## Building from Source

### Requirements

- **Rust** ≥ 1.80 (install from [rustup.rs](https://rustup.rs) if needed)
- **Nix** (optional but recommended on NixOS) — see [nix.dev](https://nix.dev)
- **clang** and **development headers** for Wayland/X11 support

### Clone the repository

```bash
git clone https://github.com/gfriloux/pgpilot.git
cd pgpilot
```

### Build in the Nix dev shell (recommended)

If you have `nix` installed:

```bash
nix develop
cargo build --release
```

The dev shell sets up `LIBCLANG_PATH` and `LD_LIBRARY_PATH` for all dependencies (sequoia-openpgp, iced/Wayland, dbus, etc.).

### Build without Nix

```bash
cargo build --release
./target/release/pgpilot
```

**Note**: On systems without a pre-configured environment, you may need to install additional packages (clang, libssl-dev, libdbus-1-dev, libwayland-dev, etc.). See your distribution's docs if the build fails.

---

## Verification

Once installed, verify pgpilot is working:

1. Launch pgpilot:
   ```bash
   pgpilot
   ```

2. A window should appear. If not, check that your display server (X11 or Wayland) is running:
   ```bash
   echo $DISPLAY     # X11
   echo $WAYLAND_DISPLAY  # Wayland
   ```

3. Open the **Diagnostic** page (sidebar, "Diagnostic" button):
   - All checks should show "OK" or "Info" (green/blue icons)
   - If you see "Error" in red, follow the troubleshooting guide (see [Troubleshooting](/pgpilot/troubleshooting/))

---

## Uninstall

To remove pgpilot:

```bash
# If installed via /usr/local/bin/
sudo rm /usr/local/bin/pgpilot

# To also remove config and cached data:
rm ~/.config/pgpilot/config.yaml
```

**Note**: pgpilot does not modify your GPG keyring directly — all operations delegate to the `gpg` binary. Your keys remain in `~/.gnupg/` and are safe to back up independently.

---

## Removing GnuPG keyring data

> **Warning: This is NOT part of uninstalling pgpilot.**
>
> `~/.gnupg/` is GnuPG's own data directory. It contains **all your GPG keys**, including keys from other applications, keys you've imported over years, and your private keys. Deleting it is **permanent and irreversible** unless you have a backup.
>
> pgpilot does not own or modify this directory — it delegates all operations to the `gpg` binary which reads from `~/.gnupg/`.

Only proceed if you intend to **completely destroy your GPG keyring** and have verified your backups.

**Prerequisites before proceeding:**

1. Export and securely store all private keys you want to keep
2. Back up your revocation certificates
3. Verify your backups can be restored

```bash
# IRREVERSIBLE — destroys all GPG keys
rm -rf ~/.gnupg/
```

---

## Next steps

- Read [Quickstart](/pgpilot/quickstart/) to create your first key
- See [Key Management](/pgpilot/key-management/) for detailed workflows
- Check [Troubleshooting](/pgpilot/troubleshooting/) if you encounter issues
