name: Deploy docs (legacy mdbook — DISABLED)
# This workflow is disabled. Documentation is now built with Astro via docs.yml.
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled — use docs.yml instead'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use docs.yml."
